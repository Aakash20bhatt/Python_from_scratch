def checkPalindrome(num):
    temp=num
    rev=0
    while num>0:
        rem=num%10
        rev=rev*10+rem
        num=num//10
    if temp==rev:
        return True
num = int(input())
isPalindrome = checkPalindrome(num)
if(isPalindrome):
	print('true')
else:
	print('false')
