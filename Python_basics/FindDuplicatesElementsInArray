def duplicate_find(li):
    for i in range (len(li)):
        for j in range(len(li)):
            if (li[i]==li[j] and i!=j):
                return li[j]

                       
                       
                       
                       
n=int(input())
li=[int(i) for i in input().split()]
print(duplicate_find(li))
